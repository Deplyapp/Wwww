services:
  - type: web
    name: node-web-app
    runtime: docker
    dockerfilePath: ./Dockerfile
    healthCheckPath: /health
    envVars:
      - key: SSH_USER
        value: vpsuser
      - key: SSH_PASSWORD
        value: SuperSecure@VPS2024!
      - key: TUNNEL_MODE
        value: cloudflare
